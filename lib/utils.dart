import 'package:flutter/material.dart';

const Color surfaceColor = Color(0xFF232323);
const Color gradientLight = Color(0xFF5BB8DB);
const Color gradientDark = Color(0xFF4166C8);

extension Sum on int {
  int plus(int other) => this + other;
}
